# 올공(ALLGONG) — allgongin.com 저장소 작업 규칙

이 저장소는 공공기관 채용포털 **올공(allgongin.com)** 의 소스입니다.
`index.html` 하나가 사이트 전체이며, `automation/` 은 GCP Cloud Function
(채용공고 크롤링 + SNS/쇼츠/뉴스 자동화)입니다.

## ⚠️ 절대 변경 금지 — 확정 문구 (소유자 지시, 2026-08)

아래 문구는 소유자가 확정한 표현이다. **어떤 코드 수정·리팩터링·문구 생성
작업 중에도 절대 이전 표현으로 되돌리거나 다르게 바꾸지 말 것.**

1. 운영자 소개 문구는 반드시 **"공공기관 경영평가위원 출신 교수"** 로 표기한다.
   - 금지: "현직 교수", "경영평가위원으로 활동 중인" 등 현직으로 오해될 표현
   - 적용 위치: 헤더 슬로건, 올공몰 부제, 제휴 카드(aff-badge), 회사소개 문서,
     product_inform.html, SNS 자동 댓글 문구(automation/social_post.py) 등 전부
2. 회사 주소는 반드시 **"(34430) 대전광역시 대덕구 한남로70 (오정동133) 경상대학"**
   까지만 표기한다. — **"315호실" 등 호실 정보를 다시 붙이지 말 것.**
   - 적용 위치: 푸터 회사정보, 회사소개/이용약관 등 푸터 문서 전부

## 기타 상시 규칙 (기존 대화에서 확정)

- **PC(웹) 레이아웃은 요청받은 부분 외 절대 변경 금지.** 모바일 전용 스타일은
  `html.mobile-view` 스코프(문서 하단 `#mobile-responsive-css`)에만 작성한다.
- 지시받은 것 이외의 코드·레이아웃은 건드리지 않는다.
- 커밋/PR/코드 어디에도 모델 식별자를 넣지 않는다.
- 비밀값(API 키·토큰)은 코드/저장소에 저장하지 않는다 — Secret Manager 또는
  Firestore `_config/*` 문서 사용.
- `claude/**` 브랜치에 푸시하면 GitHub Action 이 main 으로 복사되어
  GitHub Pages(allgongin.com)에 즉시 반영된다 — 푸시 = 실서비스 배포임을 유의.
